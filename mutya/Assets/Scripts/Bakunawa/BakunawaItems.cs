using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BakunawaItems : MonoBehaviour
{
    public float tikbalang_item;
    // Start is called before the first frame update
    void Start()
    {
        tikbalang_item = 0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
